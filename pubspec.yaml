name: yield_effect_handle_then_resume
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.0

dependencies:
  meta: ^1.19.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
